# App flow

- Customer: opens the restaurant feed, checks crowd and wait time, then opens a restaurant detail page.
- Manager: opens the manager dashboard, views the layout placeholder, and reviews analytics.
- Employee: uses the employee dashboard to view table status and the queue. The update buttons are placeholders for a future live workflow.

All current information comes from `lib/mock-data.ts`.
